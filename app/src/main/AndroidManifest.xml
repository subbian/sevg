<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sevg">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SEVG"
        android:networkSecurityConfig="@xml/network_security_config">

        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.SEVG.NoActionBar"
            android:label="@string/app_name">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity android:name=".SalesAndInventoryActivity"
            android:theme="@style/Theme.SEVG.NoActionBar"
            android:windowSoftInputMode="stateAlwaysHidden|stateVisible|adjustPan" />

        <activity android:name=".SalesDetailsActivity"
            android:theme="@style/Theme.SEVG.NoActionBar"
            android:windowSoftInputMode="stateVisible|adjustPan" />

        <activity android:name=".InventoryItemDetailsActivity"
            android:theme="@style/Theme.SEVG.NoActionBar"
            android:windowSoftInputMode="stateVisible|adjustPan" />

        <activity android:name=".AddLineItemActivity"
            android:theme="@style/Theme.SEVG.NoActionBar"
            android:windowSoftInputMode="stateAlwaysHidden|stateVisible|adjustPan" />

    </application>

</manifest>